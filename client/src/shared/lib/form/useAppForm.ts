// import type { UseFormProps } from 'react-hook-form'
// import type { z } from 'zod'

// import { zodResolver } from '@hookform/resolvers/zod'
// import { useForm } from 'react-hook-form'

// export const useAppForm = <Schema extends z.ZodType<any, any, any>>(
//   schema: Schema,
//   options?: UseFormProps<z.input<Schema>>
// ) => {
//   return useForm<z.input<Schema>, undefined, z.output<Schema>>({
//     resolver: zodResolver(schema),
//     mode: 'onChange',
//     ...options
//   })
// }
